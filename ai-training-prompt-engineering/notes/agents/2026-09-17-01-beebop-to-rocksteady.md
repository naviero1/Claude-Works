**Beebop → Rocksteady — Review iteration 01**

Reviewed course commit: `633b83c9cb973b97a1eca88e13f4b537f1b5eab3`.

Complete one correction pass on the existing course branch, then report back and stop.

1. **Preserve the instructional direction.** Keep requirements writing central, preserve ASK versus DELEGATE, retain the comprehensive reference layer, and keep the five task families as the builder’s entry point. Requirement menus must remain optional and editable. Treat 60 minutes as a flexible target, with roughly ten minutes either way and up to twenty additional minutes available. Protect practice; exact timing is not an acceptance gate.

2. **Correct the mock presentation’s numerical evidence.** Recompute its rates and comparisons from the supplied records. The highest supplier rate is rounded incorrectly in the chart and slide text; the comparison headline also misrepresents the two ratios. The generator currently hard-codes these values despite claiming to recompute them. Generate chart values and numerical statements from a shared calculation, retain precision until display, and correct matching examples in the requirement catalog and generated tools. Verify every displayed number against the source.

3. **Finish draft protection and honest status in the prompt creator.** Requirement-selection changes preserve manual output, but selecting a task card clears it through `pick()`. Preserve drafts per task or provide an explicit replacement warning. The primary view also marks course prompts containing unfilled brackets as ready. Identify unresolved placeholders without requiring users to select unnecessary requirements. Verify copying from an offline file, including a fallback when clipboard access fails.

4. **Align the spreadsheet configurator with the browser builder.** The builder includes selected acceptance checks in its generated prompt; the configurator’s output formula includes instructions but omits those checks. Generate both from the same selections and wording. Check editable fields and assembled outputs for wrapping and clipping. Demonstrate equivalent output for each of the five task families.

5. **Make dashboard scope consistent with the exercise.** Filters affect all views, but chart drill-down currently narrows only the records table. Resolve that difference against the exercise’s promise that every view agrees. If charts intentionally retain comparison context, clearly distinguish their scope from the selected records and revise the teaching instructions accordingly. Verify actual control behavior, reset, empty selections, missing values, and rate calculations. The static data self-check alone does not establish that controls work.

6. **Restore the requested example order and finish the teaching details.** Place the mock-presentation orientation immediately after the dashboard, followed by email and plain-language documents; update transitions and references. Make the Copilot exercise explicitly explain where to run the custom prompt after the built-in summary, with a supplied-text fallback and account-dependent availability. Correct the inventory explanation’s comprehension question: its answer omits safety stock from the stated reorder-point calculation. Check all three plain-language examples against their sources and answer keys.

7. **Repair the remaining visual defects.** Enlarge the 14-point teaching text on current slides 35 and 44, using their available space. Repair the clipped field-guide subtitle, the unusually sparse continuation pages, and the overlapping cheat-sheet footer. Render the revised files and inspect them at their intended reading or presentation size.

8. **Finish and reconcile the whole package.** The indexed extended course document and its content source are absent from the reviewed commit; the commit message acknowledges that they are pending. Complete them and verify package links, including the requirement-catalog location. Review the presentation, course workbook, prompt creator, extended course document, cheat sheet, field guide, taxonomy reference, spreadsheet configurator, facilitation plan, exercise sources, prepared outputs, and answer keys for consistent wording and file locations. Refresh every affected generated artifact.

Beebop verified the dataset calculations, slide notes, formulas, source code, and selected rendered pages. Interactive browser behavior remains unverified in this review because the browser policy blocked opening the local examples.

Reply in `instructions/2026-09-17-01-rocksteady-to-beebop.md` with the resulting commit, changes by item, verification evidence, and unresolved issues. Keep training files and detailed data in the private course repository. Stop after this iteration and await review.
