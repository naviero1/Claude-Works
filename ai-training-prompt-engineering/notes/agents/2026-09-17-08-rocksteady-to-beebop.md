# Rocksteady → Beebop · 08 · Iteration-06 understanding (owner-approved, executing)

Date: 2026-09-17 · The owner reviewed this understanding in session and approved execution.

## What I will do

1. **Freeze everything but the presentation.** Course PDF (the 83-page all-in-one),
   workbook, builder, configurator, dashboard, prepared outputs, indexes, and
   reference documents stay exactly as committed; synchronization happens in a
   later approved iteration, once the slides are accepted.
2. **Restore only** `deliverables/From_Prompts_to_Agents_Facilitated_60_Minute.pptx`
   from `50c729e` (verified locally: 101 slides; `ba57f77` avoided as instructed).
   File-level restoration committed normally — no history rewrite.
3. **Retire the pass-05 method.** The three scripts will not run again. One new
   deterministic rebuild source with repository-relative paths; pristine baseline
   kept; work happens on a separate copy until validation passes; no XML-level
   surgery, no donor duplication, no coordinate-inferred roles, no run flattening.
4. **Rebuild slides 33–44 in place** (101 slides stays 101), using your visible
   copy per slide, the existing visual system per mode (DIVIDER dark · TEACH teal
   · DO amber), one dominant message, at most two content regions, body ≥18–20pt,
   no visible prompt over ~120 words, acronyms expanded at first use, and
   consistent chrome. Facilitation language, full prompts, expected results,
   verification values, mistakes, and extensions go to speaker notes (standard
   MODE/TIME/PURPOSE/LAND THIS/SAY/DO/BRIDGE/IF LATE fields); no repository
   paths, tab names, fallback filenames, instructor-key language, or answer
   values on visible slides (the dashboard check value moves to notes).
5. **Verify before committing:** render all 101 slides; individually inspect the
   twelve rebuilt slides and a 12-up contact sheet; confirm slides 1–32 and
   45–101 are unchanged from the baseline; confirm modes/accents/titles/notes
   agree; font-floor, word-count, acronym, no-answers, and no-"five task
   families" checks; then a manual LibreOffice open of the final file.
6. **Report and stop:** the commit, baseline confirmation, final titles and
   modes for 33–44, renders + contact sheet, check results, and unresolved
   items — then await your review and the owner's final word.

## Known temporary consequences (will be listed as open items)

- Restoring the baseline reverts the citation cleanup and the Role & Task
  typography repair on non-target slides until the sync iteration.
- The course-log study aid and the old quotation-extension page leave the deck
  (their content remains in the frozen workbook and course PDF).
- Your condensed visible prompts will differ in wording from the frozen
  workbook's canonical prompt texts until the sync iteration reconciles them
  (notes will carry the full canonical prompts, extended per your field notes
  for the quotation step).

— Rocksteady
